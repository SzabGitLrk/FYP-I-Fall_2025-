<% layout("/layouts/boilerplate") %>

<div class="container mt-5 d-flex justify-content-center">
  <div class="col-md-6" style="max-width: 620px;">
    <div class="card shadow-lg border-0 rounded-4">
      <div class="card-body p-4">

        <h3 class="text-center mb-4 text-success fw-bold">Register Your NGO</h3>

        <% if (error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <% if (success) { %>
          <div class="alert alert-success"><%= success %></div>
        <% } %>

        <form action="/ngo_signup" method="POST" enctype="multipart/form-data">

          <div class="mb-3">
            <label class="form-label">NGO Name</label>
            <input type="text" class="form-control" name="ngo_name" required>
          </div>

          <div class="mb-3">
            <label class="form-label">License Number</label>
            <input type="text" class="form-control" name="licence_number" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Official Email</label>
            <input type="email" class="form-control" name="email" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Contact Number</label>
            <input type="text" class="form-control" name="contact_number" required>
          </div>

        <div class="mb-3">
  <label class="form-label">Complete Address</label>
  <input type="text" class="form-control" name="address" 
         placeholder="e.g. 123 Main St, Block 5" required>
  <small class="text-muted">Do not use 'near' or 'opposite'. Use street names/landmarks.</small>
</div>

<div class="mb-3">
  <label class="form-label">City</label>
  <input type="text" class="form-control" name="city" 
         placeholder="e.g. Karachi" required>
</div>

          <div class="mb-3">
            <label class="form-label">Head Person Name</label>
            <input type="text" class="form-control" name="head_person_name" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Head Person Contact</label>
            <input type="text" class="form-control" name="head_person_contact" required>
          </div>

          <div class="mb-3">
            <label class="form-label">Verification Documents</label>
            <input
              type="file"
              class="form-control"
              name="verification_documents"
              multiple
              accept=".pdf,.jpg,.jpeg,.png"
              required
            >
            <small class="text-muted">Upload NGO registration certificate, license, or official proof (PDF, JPG, JPEG, PNG).</small>
          </div>

          <div class="mb-3">
            <label class="form-label">Create Password</label>
            <input type="password" class="form-control" name="password" required>
          </div>

          <div class="d-grid mt-4">
            <button type="submit" class="btn btn-success btn-lg">Register NGO</button>
          </div>

          <p class="text-center mt-3 text-muted small">
            After registration, your account will remain <strong>Pending</strong> until admin approval.
          </p>

        </form>

      </div>
    </div>
  </div>
</div>
