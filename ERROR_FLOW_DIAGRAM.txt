═══════════════════════════════════════════════════════════════════════════════
                        ERROR FLOW ANALYSIS DIAGRAM
═══════════════════════════════════════════════════════════════════════════════


BEFORE FIX (Current State - Getting Error):
───────────────────────────────────────────────────────────────────────────────

User uploads image
        ↓
FormData created with File ✓
        ↓
POST /api/images/upload ✓
        ↓
[BACKEND - OLD CODE]
FileInterceptor extracts file ✓
        ↓
ImageService.uploadImage()
        ├─→ Check for AWS credentials
        ├─→ Find: AWS_ACCESS_KEY_ID = "" (EMPTY)
        ├─→ Find: AWS_SECRET_ACCESS_KEY = "" (EMPTY)
        ├─→ Try to use AWS S3 (OLD CODE LOGIC)
        ├─→ ❌ S3 SDK fails: "AccessDenied" or similar
        └─→ Error thrown, caught by try/catch
        ↓
ImageController catch block
        ├─→ Error converted to 500
        └─→ Returns: {error: "..."}
        ↓
Frontend receives 500 error
        ↓
apiClient error interceptor catches 500
        ├─→ Returns generic message:
        └─→ "We could not reach the server right now"
        ↓
❌ USER SEES ERROR


AFTER FIX (After Restarting Backend):
───────────────────────────────────────────────────────────────────────────────

Backend restarts
        ├─→ Recompiles TypeScript → JavaScript
        ├─→ Loads NEW image.service.ts code
        └─→ Starts with new logic
        ↓
User uploads image
        ↓
FormData created with File ✓
        ↓
POST /api/images/upload ✓
        ↓
[BACKEND - NEW CODE]
FileInterceptor extracts file ✓
        ↓
ImageService.uploadImage()
        ├─→ Check for AWS credentials
        ├─→ Find: AWS_ACCESS_KEY_ID = "" (EMPTY)
        ├─→ useLocalStorage = true
        ├─→ ✓ Use local file storage INSTEAD
        ├─→ Create /uploads directory if needed
        ├─→ fs.writeFileSync() → File saved ✓
        ├─→ Generate URL: http://localhost:3002/uploads/uuid-filename.jpg
        └─→ Return { imageUrl: "http://localhost:3002/uploads/..." }
        ↓
ImageController returns 200 OK
        ├─→ {statusCode: 200, success: true, data: {imageUrl: "..."}}
        └─→ Logs: "[ImageController] Upload successful"
        ↓
Frontend receives 200 with imageUrl
        ↓
uploadedImageUrl state set ✓
        ↓
User clicks Save
        ↓
updateUser() called with { name, image: imageUrl }
        ↓
[BACKEND] PUT /users
UserController.update()
        ├─→ Update user.image = "http://localhost:3002/uploads/..."
        ├─→ Save to database ✓
        └─→ Return updated user with image field
        ↓
Frontend receives user with image
        ↓
Query cache updated with new user data ✓
        ↓
All components see new image ✓
        ↓
Navigate to /dashboard
        ↓
Sidebar shows avatar from database ✓
        ↓
✅ SUCCESS!


ERROR DETECTION FLOW:
───────────────────────────────────────────────────────────────────────────────

Frontend Error Message: "We could not reach the server right now"
        ↓
Could mean any of these:
        ├─→ 1. Network error (can't reach server)
        ├─→ 2. 500+ error (server error)
        ├─→ 3. CORS error (cross-origin)
        └─→ 4. Timeout (server too slow)
        ↓
In OUR CASE:
        └─→ 500 error from backend
        └─→ Caused by missing AWS credentials
        └─→ OLD code tries S3, fails
        └─→ Error caught and converted to generic message


CODE CHANGE LOCATIONS:
───────────────────────────────────────────────────────────────────────────────

File: image.service.ts
├─→ OLD (line ~25):
│   this.s3 = new S3({...}) // Throws if no credentials
│
└─→ NEW:
    Check AWS credentials first
    IF MISSING → use local storage
    IF PRESENT → use S3


File: image.controller.ts
├─→ OLD: Generic error logging
└─→ NEW: Detailed logging with [ImageController] tags


File: main.ts
├─→ OLD: No static file serving
└─→ NEW: useStaticAssets() for /uploads directory


File: user.dto
├─→ OLD: No image field support
└─→ NEW: Added image?: string field


WHY RESTART REQUIRED:
───────────────────────────────────────────────────────────────────────────────

TypeScript Source Files (*.ts)
    ↓ (Only when running "npm run start:dev")
Compiled JavaScript Files (*.js)
    ↓
Running Node.js Process
    ↓
Backend API Responses

If you change .ts but don't restart:
    ├─→ .ts file has new code ✓
    ├─→ .js file has old code ✗
    └─→ Running process uses old .js ✗
    └─→ Results: Same error occurs


VERIFICATION AFTER RESTART:
───────────────────────────────────────────────────────────────────────────────

✓ Check 1: Backend console should show
    [Nest] 12345 - 06/22/2026, 10:30:45 AM   [NestFactory] 
    [Nest] 12345 - 06/22/2026, 10:30:45 AM   [InstanceLoader]
    [Nest] 12345 - 06/22/2026, 10:30:45 AM   Bootstrap successful

✓ Check 2: Backend console should show
    [ImageService] Initializing...
    [ImageService] Using local file storage at: C:\...\uploads

✓ Check 3: Upload image and check console
    [ImageController] Upload request received {file: {...}}
    [ImageService] File saved to: ...
    [ImageService] Returning URL: ...

✓ Check 4: File exists
    dir fyndbox-backend\uploads\
    (Should list uploaded files)

✓ Check 5: Database updated
    SELECT id, image FROM "user" WHERE id = '...';
    (Should show image URL)


═══════════════════════════════════════════════════════════════════════════════
